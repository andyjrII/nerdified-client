# Backend API Configuration
# The base URL of your backend API
# Development: http://localhost:3100/api
# Production: https://api.yourdomain.com/api
# Note: NEXT_PUBLIC_ prefix is required for Next.js to expose this to the browser
NEXT_PUBLIC_BASE_URL=http://localhost:3100/api

# Next.js Configuration
# The app runs on port 3101 (configured in package.json dev script)
# Override: PORT=3101 npm run dev

# Environment
# Set to 'development' or 'production'
# This is automatically set by Next.js based on NODE_ENV
# NODE_ENV=development

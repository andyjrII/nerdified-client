# Course default image

Place **course.jpeg** here to use as the default course image when no image is uploaded.

If you have an image in another folder in the project (e.g. `image/course.jpeg`), copy it here:

```bash
cp /path/to/course.jpeg ./public/images/course.jpeg
```
